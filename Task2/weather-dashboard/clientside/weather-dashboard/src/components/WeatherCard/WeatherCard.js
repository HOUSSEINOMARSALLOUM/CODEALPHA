import React from "react";
import "./WeatherCard.css";

const WeatherCard = ({ data }) => {
  // Include logic to display weather data
  return (
    <div className="weather-card">
      <h2>City Name</h2>
      {/* Display weather information here */}
    </div>
  );
};

export default WeatherCard;
